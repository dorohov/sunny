	
	/*
	$(
		function() {
			
			var nav = $('.navbar').eq(0);
			var pcc = $('.page-content-cont').eq(0);
			
			var h = pcc.outerHeight(true) - nav.outerHeight(true);
			
			$('.page-content-cont .content-scroll').css({
				height : pcc.outerHeight(true) + 'px',
			});
			
			$('.page-content-cont .scroll-hide').css({
				height : h + 'px',
			});
			$('.page-content-cont .scroll-element').css({
				height : h + 'px',
			});
			$('.page-content-cont .scroll-overflow').css({
				height : h + 'px',
			});
		}
	);
	*/